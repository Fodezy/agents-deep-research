### Quick QA of the HYBRID-01 deliverables

| Area                                | What Looks Solid                                                                                                                                                                      | Small Gaps / Edge-Cases to Tackle in HYBRID-02                                                                                                                                                                                                                        |
| ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Schema set (+ registry)**         | • All four JSON Schemas include version locking and `additionalProperties:false` – great. <br>• `schemas/registry.json` maps every agent → schema → Outlines fn so code can stay DRY  | • Registry hard-codes only **v1**. Add a `latest_version` helper in `validator.py` so new versions auto-resolve.<br>• `chunk_summary`’s `key_points` `minLength:10` might clip bullet keywords; consider 5-8.                                                         |
| **Validator utility**               | • Centralised load & validate function is clean                                                                                                                                       | • Schemas are enumerated in a dict – if you drop a new file you must edit code. Use `glob('*.json')` + `$id` or filename pattern to auto-discover.<br>• Return type today is `(bool, str)`; expose the caught `jsonschema.ValidationError` object for richer logging. |
| **Malformed sample set**            | • Covers parsing, schema and “function ignored” cases                                                                                                                                 | • Add at least one sample where the model **splits field names over newlines** (the `schema_v\non` bug) so the wrapper can learn to repair newline-noise.                                                                                                             |
| **Integration notes**               | • Clear recipe for wrapping Outlines into agent factories                                                                                                                             | • Remember to **return the Outlines function reference** in the agent so you can call `.call()` during `ResearchRunner`.                                                                                                                                              |
| **Implementation plan (HYBRID-02)** | • ValidationWrapper + Observability class skeletons are sketched                                                                                                                      | • Pydantic models per schema not yet written—needed for `_validate_with_pydantic`.<br>• `llm_config.call_chat()` spec is stubbed; be sure to honour `functions` when `ENABLE_FUNCTION_CALLING=true`.                                                                  |

---

### Guard-Rails to Keep HYBRID-02 Focused

1. **Scope freeze:** HYBRID-02 **only** delivers

   * `ValidationWrapper` (working, not perfect)
   * `ObservabilityHandler` with in-memory counters
   * `llm_config` ability to pass `functions` list
   * Unit tests for three malformed samples.
   * No agent refactors yet (they’re HYBRID-05+).

2. **LoC budget:**

   * `validation_wrapper.py` ≤ 200 LoC including helper methods.
   * `observability.py` ≤ 120 LoC.

3. **Success gates (must-pass CI):**

   * `pytest -q tests/test_validation_wrapper.py` returns green.
   * Wrapper repairs **≥ 2/3** malformed samples.
   * `validator.list_schemas()` auto-detects all files in `schemas/`.

4. **Out-of-scope (punt to HYBRID-03+):**

   * Prometheus integration → metrics stub only.
   * Fancy exponential-back-off retries (single retry rule stands).
   * Full Pydantic model hierarchy (only for schemas touched in HYBRID-02).

5. **Due-date check-ins:**

   * **Day 2:** demo wrapper validating & repairing `json_parsing_errors`.
   * **Day 4:** all three malformed samples, metrics counters printed.
   * **Day 5:** merge PR, tag `v0.2.0`, hand-off to HYBRID-03 team.

---

### Immediate To-Do

1. **Finish Pydantic dataclasses** mirroring `select_tools_v1.json`, `report_gaps_v1.json`, etc.
2. Build the **wrapper prototype** against those models.
3. Wire a **tiny CLI script** (`scripts/validate_sample.py`) so reviewers can run:

   ```bash
   python scripts/validate_sample.py malformed_samples.json
   ```
4. Push branch **`feature/HYBRID-02-validation-wrapper`** and open a draft PR for early review.

You’re right on target—let’s keep momentum by landing HYBRID-02 before adding any new bells and whistles.
